'use client';

import React from 'react';
import { PlaceholderPage } from '@/components';

export default function CreateConversationPage() {
  return (
    <PlaceholderPage
      title="Conversational creation"
      description="Chat-like creation for affirmations, meditations, rituals. (Stub — state machine, no static forms)"
      backHref="/create"
    />
  );
}
