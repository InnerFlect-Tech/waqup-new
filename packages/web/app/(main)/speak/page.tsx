'use client';

import React from 'react';
import { PlaceholderPage } from '@/components';

export default function SpeakPage() {
  return (
    <PlaceholderPage
      title="Speak"
      description="Orb that speaks — voice-first conversation UI. (Stub — Phase 9, voice-interaction-design)"
      backHref="/home"
    />
  );
}
