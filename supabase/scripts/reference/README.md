# SQL Reference Scripts

These SQL files are **reference only**. Canonical migrations live in `supabase/migrations/`.

They document the intended schema for content_items and orb-related tables. Use for documentation or manual setup only.
